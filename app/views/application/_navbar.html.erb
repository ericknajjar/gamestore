<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
     </div>

    <div id="navbar" class="navbar-collapse collapse">

      <%= link_to image_tag("brazilian_flag.gif"), url_for(params.merge({:locale => 'pt'}))  %>
      <%= link_to image_tag("american_flag.png"),url_for(params.merge({:locale => 'en'})) %>

      <%= link_to 'FooStore', { controller: "site", action: "index" }, class: 'navbar-brand' %>

      <ul class="nav navbar-nav navbar-right">
        <% if is_admin? %>

          <li> <%= link_to 'Admin', { controller: "games", action: "index" } %> </li>
          <li> <%= link_to 'Logout', { controller: "sessions", action: "destroy" }%>  </li>
  
        <%else%>
              <li> <%= link_to 'Login', { controller: "sessions", action: "create" }%> </li>
        <%end%>
      </ul>
     
    </div>
  </div>

</nav>